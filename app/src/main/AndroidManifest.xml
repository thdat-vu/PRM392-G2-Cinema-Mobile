<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRM392G2MovieBookingMobileApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.bookings.FoodDrinksActivity"
            android:exported="false" />
        <activity
            android:name=".ui.bookings.BookingHistoryListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.bookings.BookingHistoryInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.payment.PaymentActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="app"
                    android:scheme="demozpdk" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MovieListActivity" />
        <activity android:name=".ui.MovieDetailActivity" />
        <activity android:name=".ui.tickets.TicketsActivity" />
        <activity android:name=".ui.tickets.TicketDetailActivity" />
        <activity android:name=".ui.ShowtimeSelectionActivity" />
        <activity android:name=".ui.bookings.SeatActivity" />
        <activity
            android:name=".ui.payment.PaymentNotificationActivity"
            android:exported="true" />
    </application>

</manifest>